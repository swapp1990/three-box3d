# Changesets

This folder holds changesets for package releases. The three published packages
are versioned together through the `fixed` group in `config.json`.

Run `npx changeset` to add a changeset for user-facing changes. Run
`npx changeset version` to apply pending changesets to package versions and
changelogs before publishing.
