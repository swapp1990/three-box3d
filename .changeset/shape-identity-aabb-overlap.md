---
"box3d-web": minor
---

Add shape-identity contact events (`WithShapes` drains + `getShapeIdentity`), AABB
overlap queries (`overlapAABB`/`overlapAABBInto`), `FixedStepper.stepOnce` +
`telemetry()`, atomic native registry rollback on exhaustion, and SIMD-default
WASM with a named compat variant.
