---
"box3d-web": minor
---

Add opt-in scene-composition helpers: a deterministic identity system, the
scene-participant contract, an entity registry, a scene runtime with a reset
barrier, transactional spawn and a registration host, and native-limit
constants (`MAX_NATIVE_SHAPE_INDEX` / `MAX_NATIVE_WORLDS` /
`MAX_NATIVE_GENERATION`).
